# FileStore SMB mount settings for Cursor agent hooks.
# Adjust if your share name or mount point differs.

FILESTORE_MOUNT_POINT="/Volumes/FileStore"
FILESTORE_VERIFY_PATH="/Volumes/FileStore/ASTRO"
FILESTORE_SMB_URL="smb://Mac Studio/FileStore"
FILESTORE_MOUNT_TIMEOUT=30
